<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: linear-gradient(to bottom, #33ccff, #ff99cc);
        }
        
        .welcome-container {
            width: 500px;
            margin: 100px auto;
            padding: 20px;
            border: 1px solid #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-image: url('background.jpg');
            background-size: cover;
            animation: fadeIn 2s;
        }
        
        .welcome-message {
            font-size: 24px;
            font-weight: bold;
            color: #e74c3c;
            animation: slideIn 2s;
        }
        
        .sub-message {
            font-size: 18px;
            color: #2ecc71;
            animation: fadeInUp 2s;
        }
        
        button {
            background-image: linear-gradient(to bottom, #4CAF50, #3e8e41);
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            animation: pulse 2s infinite;
        }
        
        button:hover {
            background-image: linear-gradient(to bottom, #3e8e41, #2ecc71);
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        
        @keyframes slideIn {
            from {
                transform: translateX(-100px);
            }
            to {
                transform: translateX(0);
            }
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="welcome-container">
        <h1 class="welcome-message">Welcome!</h1>
        <p class="sub-message">We're glad you're here.</p>
        <button onclick="location.href='nextpage.html'">Continue</button>
    </div>
</body>
</html>
